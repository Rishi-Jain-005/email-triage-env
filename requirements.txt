# Core server dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
websockets>=12.0

# HTTP client (used by client.py and inference.py)
requests>=2.31.0
httpx>=0.27.0

# LLM client (required by inference.py per competition rules)
openai>=1.30.0

# Utilities
python-dotenv>=1.0.0
pytest>=8.0.0